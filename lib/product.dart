import 'package:flutter/material.dart';

class Product {
  String name;
  String description;
  int price;
  int quantity;
  // photoDataType photo;

  Product(this.name, this.description, this.price, this.quantity);
}